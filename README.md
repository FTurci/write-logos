# Write Logos in  Python

Write to SVG with a custom font.

The program writes first an svg that includes a reference to the local font (`in.svg`) and then uses `inkscape` to convert the svg to a pure path `out.svg`.
